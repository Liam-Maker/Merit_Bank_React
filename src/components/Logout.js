import React from 'react'

function Logout() {
    return (
        <div>
            <h1>Welcome <span>John</span></h1>
            <button>Logout</button>
        </div>
    )
}

export default Logout
